// Converting Strings to Numbers
// Converting Numbers to Strings
// Converting Dates to Numbers
// Converting Numbers to Dates
// Converting Booleans to Numbers
// Converting Numbers to Booleans

const num1 = 0
const num2 = 20;
const result = Number(num2) + num1

const toString = String(num1)
const toString2 = String(num2)

const toBoolean = Boolean(" ")
const toNumber = Number(10.5)
const toBigInt = BigInt(10)
const toSymbol = Symbol("ini adalah simbol deskripsi")
const toInt = parseInt("10.001")
const toFloat = parseFloat(String(num2))

console.log( toFloat)